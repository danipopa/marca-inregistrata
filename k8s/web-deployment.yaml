apiVersion: apps/v1
kind: Deployment
metadata:
  name: marca-web
  namespace: inregistrare-marca
  labels:
    app.kubernetes.io/name: marca-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: marca-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: marca-web
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: web
          image: ghcr.io/danipopa/inregistrare-marca-web:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: marca-web-config
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
