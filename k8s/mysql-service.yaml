apiVersion: v1
kind: Service
metadata:
  name: marca-mysql
  namespace: inregistrare-marca
  labels:
    app.kubernetes.io/name: marca-mysql
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: marca-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
